#include <ros/ros.h>
int main(int argc, char *argv[])
{
    ros::init(argc, argv, "sensor_node");
    // code for setup
    while (ros::ok())
    {
        // code for loop
    }
    // code for end
    return 0;
}